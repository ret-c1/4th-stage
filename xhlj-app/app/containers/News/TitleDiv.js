import styled from 'styled-components';

const TitleDiv = styled.div`
    padding-left: 15px;
    line-height: 2;
    /* flex-basis: auto; */
    width: calc(100% - 140.4px);
`;

export default TitleDiv;
