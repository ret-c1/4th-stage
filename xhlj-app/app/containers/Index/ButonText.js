import styled from 'styled-components';

const ButonText = styled.div`
    display: inline-block;
    font-size: 14px;
    background: linear-gradient(to top, #8f8f8f, #bdbdbd);
    -webkit-background-clip: text;
    color: transparent;
`;

export default ButonText;
