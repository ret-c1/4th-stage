import styled from 'styled-components';

const ZhiboText = styled.div`
    display: inline-block;
    margin-left: 5px;
    position: relative;
    top: 2px;
    font-size: 14px;
`;

export default ZhiboText;
