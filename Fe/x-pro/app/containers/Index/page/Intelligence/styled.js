import styled from 'styled-components';
import { Card } from 'antd';

export const ScCard = styled(Card)`
    margin: 14px 24px;
`;
export const ScEllipsis = styled.div`
    text-overflow: ellipsis;
    white-space: nowrap;
    overflow: hidden;
`;
